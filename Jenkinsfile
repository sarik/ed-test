

def agentLabel = null
node(label: 'do-slave') {
    stage('Checking do-slave') {
        checkout scm
        
        agentLabel = 'builder'
        
        echo "do-slave done"
        
        
        
        
    }
}
